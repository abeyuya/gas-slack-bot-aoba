version: '3'
services:
  node:
    image: node:8.10
    volumes:
      - .:/var/repo
    environment:
      CLASPRC: ${CLASPRC}
      CLASPCONF: ${CLASPCONF}
      SLACK_TOKEN: ${SLACK_TOKEN}
      TWITTER_KEY: ${TWITTER_KEY}
      TWITTER_SECRET: ${TWITTER_SECRET}
    working_dir: /var/repo
